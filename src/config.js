const config = {
    api:{
        // URL: 'http://localhost:10000' // Cambia la URL para que incluya la ruta completa al endpoint de tu API
        URL: 'https://clinivetsena.onrender.com'
    }
};

export default config;